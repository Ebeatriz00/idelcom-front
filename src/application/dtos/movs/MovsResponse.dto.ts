export interface MovClasResponseDto {
  movClasId: number;
  abrv: string;
}

export interface MovOperResponseDto {
  movOperId: number;
  abrv: string;
}

export interface MovPerResponseDto {
  movPerId: number;
  abrv: string;
}

export interface MovSunatResponseDto {
  movSunatId: number;
  abrv: string;
}

export interface MovVisResponseDto {
  movVisId: number;
  abrv: string;
}